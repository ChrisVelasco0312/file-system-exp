{
  "name": "file-system-exp",
  "private": true,
  "main": "public/electron.js",
  "homepage": "./",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron-dev": "concurrently \"cross-env BROWSER=none yarn run dev\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-pack": "electron-builder -c.extraMetadata.main=build/electron.js",
    "preelectron-pack": "react-scripts build"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "cross-env": "^7.0.3",
    "electron": "^26.2.1",
    "electron-builder": "^24.6.4",
    "vite": "^4.4.5",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}

