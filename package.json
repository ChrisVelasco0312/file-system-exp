{
  "name": "file-system-exp",
  "private": true,
  "main": "public/electron.js",
  "homepage": "./",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "el:dev": "electron-vite dev -w",
    "el:build": "electron-vite build",
    "el:preview": "electron-vite preview",
    "electron-dev": "concurrently \"cross-env BROWSER=none yarn run dev\" \"wait-on http://localhost:5173 && yarn electron .\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "cross-env": "^7.0.3",
    "electron": "^26.2.1",
    "electron-builder": "^24.6.4",
    "electron-vite": "^1.0.27",
    "vite": "^4.4.5",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}
